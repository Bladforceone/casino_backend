package auth

import (
	"context"
)

func (s *serv) Register(ctx context.Context, name, login, password string) (accessToken string, sessionID string, err error) {
	//TODO implement me
	panic("implement me")
}
